# Foundation Predictions

Included directly:

- `megnet_frozen_predictions.csv`

Large prediction caches used by canonical cross-backbone, r2SCAN, WBM, and ranking experiments are enumerated in `data/manifests/large_artifacts_manifest.csv`. Deposit them with Git LFS or a DOI-backed repository before public release if they are not committed directly.
